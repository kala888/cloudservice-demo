server.port: 4100
management.context-path: /mgmt

spring:
  application:
    name: calc
hystrix:
  command:
    default:
      execution:
        isolation:
          thread:
            timeoutInMilliseconds: 10000
